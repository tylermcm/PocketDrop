PocketDrop for Linux

Run ./PocketDrop from this folder. It requires GTK 3 and libcurl, which are
already present on many desktop distributions. On Ubuntu/Debian, install them
with:

    sudo apt install libgtk-3-0 libcurl4

This build targets x86_64 Linux with glibc 2.35 or newer (Ubuntu 22.04+).
